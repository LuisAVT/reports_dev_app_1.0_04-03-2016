<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="escobedo.jnix.escobedo">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/escobedoTheme">
        <activity android:name=".mainActivity">
            <intent-filter>
                <action android:name="android.intent.action.ACTIVITY_MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".news"></activity>
        <activity android:name=".userAndPassword">
            <intent-filter>
                <action android:name="android.intent.action.ACTIVITY_USER_AND_PASSWORD" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity_Denu"></activity>
        <activity android:name=".reportes"></activity>
        <activity android:name=".panic"></activity>
        <activity android:name=".emerg"></activity>
        <activity android:name=".event"></activity>
        <activity android:name=".redes"></activity>
        <activity android:name=".contact"></activity>
        <activity android:name=".registro"></activity>
        <activity android:name=".Denu_Sospechoso"></activity>
        <activity android:name=".DenuCorru"></activity>
        <activity android:name=".DenuRobo"></activity>
        <activity android:name=".DenuAutos"></activity>
        <activity android:name=".DenuVio"></activity>
        <activity android:name=".DenuDano"></activity>
        <activity android:name=".DenuCame"></activity>
        <activity android:name=".DenuAlumbra"></activity>
        <activity android:name=".DenuLA"></activity>
        <activity android:name=".MantePla"></activity>
        <activity android:name=".DenuSema"></activity>
        <activity android:name=".DenuBaches"></activity>
        <activity android:name=".DenuSegu"></activity>
        <activity android:name=".DenuSenaVia"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"></activity>
    </application>

</manifest>
